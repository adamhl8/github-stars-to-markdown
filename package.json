{
  "name": "github-stars-to-markdown",
  "type": "module",
  "module": "index.ts",
  "scripts": {
    "lint": "tsc --noEmit && biome check --write",
    "prepare": "find .githooks -type f -exec ln -srf {} .git/hooks/ \\;"
  },
  "dependencies": {
    "ts-explicit-errors": "^2.2.0"
  },
  "devDependencies": {
    "@adamhl8/configs": "^0.8.4",
    "@biomejs/biome": "^2.0.6",
    "@types/bun": "^1.2.18",
    "typescript": "^5.8.3"
  },
  "trustedDependencies": ["@biomejs/biome"]
}
